# Data Module Schema — moved

The authoritative schema reference now lives at [`docs/SCHEMA.md`](../../docs/SCHEMA.md).

This file previously duplicated that content and had drifted: it used the
pre-rename UNIT field names (`duration`, `outcomes`, `competencies`,
`careerConnections`, `keyInquiryQuestions`) that were since standardised.
It is kept only as a pointer so existing references resolve. Do not edit
this file — update `docs/SCHEMA.md` instead.
