# HANDOFF — <short description> (<date>)

> Use this template for Level 2/3 work (see `CLAUDE.md`'s Project Rigor
> Assessment) being handed to a different session, tool, or person —
> e.g. chat-based design work handed to Claude Code for execution, a
> session handed to a fresh account or environment, or a long pause
> between sessions. Create a new dated copy each time — don't overwrite
> a prior handoff doc.

## Context

<One paragraph: what this handoff is for and why it exists — e.g.
"moving to a new account," "resuming after a break," "chat did the
design, Code should execute.">

## Locked decisions

<Bullet list of decisions that should NOT be re-litigated unless new
evidence explicitly requires it. Be specific, and include the reasoning
if it's non-obvious, so the receiving session doesn't have to guess
whether it's safe to revisit.>

## Open questions

<Anything genuinely undecided. If there are none, say so explicitly —
don't leave this section silent about whether it was considered.>

## Phased plan

<Numbered phases, each small enough to checkpoint. For Level 3 work,
each phase should end with a defined verification step before the next
phase starts.>

## What NOT to assume

<Explicitly call out facts that might seem obvious but shouldn't be
assumed true without checking — e.g. "don't assume the branch name; run
Step 0 verification first." This section exists because the worst
incidents in the source project were all cases of a true-when-written
fact being trusted instead of checked.>

## First action for the receiving session

<A single, concrete first step — e.g. "Read CLAUDE.md, then STATUS.md's
Active Threads, then this document, in that order, before proposing
anything.">
